About App

System take a excel file and convert in a HTML page.

HTML file it's formated with HTML5 and Bootstrap.

For change the data in excel the system have to reviece the lines and colums you want read.

